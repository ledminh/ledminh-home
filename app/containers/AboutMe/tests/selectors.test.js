// import { fromJS } from 'immutable';
// import { makeSelectAboutMeDomain } from '../selectors';

// const selector = makeSelectAboutMeDomain();

describe('makeSelectAboutMeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
